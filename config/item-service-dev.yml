spring:
  application:
    name: item-service
  cloud:
    config:
      override-none: true #不覆盖--server.port的端口配置(从配置中心得到的配置优先级高于--server.port)
   
    
server:
  port: 8001

eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka